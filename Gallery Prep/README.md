# Gallery Prep

公開 Gallery SPA へ載せる前の下書き・素材置き場です。

- Wiki 正本は `Backroom/{slug}/`（`gallery: true` のプロジェクト）
- 公開は [Gallery](https://github.com/YUzushio/yuzushio.github.io) 側の `@gallery-vault-receiver` で `gallery.json` に反映
- このフォルダは任意 — 表紙 PNG や export 前メモを置いてもよい
